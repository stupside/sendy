import { type FC } from 'react'

import { VideoController } from '@sendy/ui-content-video'

const VideoSimpleController: FC = () => {
  return <VideoController features={{}} />
}

export default VideoSimpleController
